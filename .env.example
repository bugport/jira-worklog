# Jira Configuration
# Required: Your Jira server URL (no trailing slash)
JIRA_SERVER=https://your-domain.atlassian.net

# Required: Your Jira email or username (only needed for Basic Auth)
# Not required if using Bearer token authentication (JIRA_USE_BEARER_TOKEN=true)
JIRA_EMAIL=your-email@example.com

# Required: Your Jira API token
# Get your API token from: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your-api-token-here

# Optional: Use Bearer token authentication instead of Basic Auth
# Set to true to use Authorization: Bearer <token> header (no username needed)
# Set to false (default) to use Basic Auth with email + token
# Bearer token authentication is more secure and doesn't require username/email
JIRA_USE_BEARER_TOKEN=false

# Optional: Rate limit for API requests (default: 5.0 requests per second)
# Limits the number of requests per second to avoid overwhelming the JIRA server
# Set to a lower value (e.g., 2.0) for slower connections or rate-limited JIRA instances
# Set to a higher value (e.g., 10.0) if your JIRA instance can handle more requests
JIRA_RATE_LIMIT=5.0

# Optional: Default project key (e.g., ABC)
JIRA_PROJECT=ABC

# Optional: SSL certificate verification (default: true)
# Set to false to bypass SSL verification (useful for self-signed certificates)
# WARNING: Only disable SSL verification in development/testing environments
JIRA_VERIFY_SSL=true

# Optional: JIRA API version (default: 'latest')
# Specify the API version to use (e.g., '1.0', '2', '3', 'latest')
# 'latest' is the recommended default - resolves to the most recent version supported by your JIRA instance
# If set, this takes precedence over JIRA_API_PATH
# Example: JIRA_API_VERSION=latest (will use /rest/api/latest - recommended)
# Example: JIRA_API_VERSION=1.0 (will use /rest/api/1.0)
JIRA_API_VERSION=latest

# Optional: Custom JIRA API path (default: auto-detected)
# Set if your JIRA API is located at a custom path
# You can specify either '/rest/api/latest' or '/api/latest'
# (The library automatically adds '/rest', so '/rest' prefix will be removed if present)
# If JIRA_API_VERSION is set, this option is ignored
# If not set, the JIRA library will auto-detect the API path from the server
JIRA_API_PATH=/rest/api/latest

